import { PayButtonVariant } from './components/PayButton';

/**
 * Дизайн кнопки оплаты.
 *
 * Чтобы поменять внешний вид кнопки во всём приложении — измените одну строку ниже.
 * Доступные варианты:
 *   'solid'    — сплошная оранжевая (в стиле остальных кнопок приложения)
 *   'gradient' — с оранжевым градиентом и тенью (яркая, привлекает внимание)
 *   'outline'  — контурная (лёгкая, ненавязчивая)
 *   'soft'     — мягкая на кремовом фоне (спокойная)
 *   'glow'     — тёмная с подсветкой (премиальный вид)
 *   'card'     — в виде «карточки» с иконкой и суммой справа
 */
export const PAY_BUTTON_DESIGN: PayButtonVariant = 'outline';

/**
 * Параметры оплаты — сумма и заголовок окна.
 * Меняйте здесь: значения подставляются в кнопки и в окно оплаты по всему приложению.
 * amount указывайте в удобном виде ("15 000 ₸") — для Kaspi из него берутся только цифры.
 */
export const PAYMENT = {
  amount: '15 000 ₸',
  title: 'Оплата за обучение',
};

/**
 * Включает встроенный переключатель дизайнов прямо в окне оплаты —
 * удобно, чтобы вживую посмотреть все варианты и выбрать нужный.
 * Для продакшена поставьте false.
 */
export const PAY_BUTTON_DESIGN_PREVIEW = false;

/**
 * Настройки оплаты через Kaspi (Kaspi Business — QR / Kaspi Pay).
 *
 * У Kaspi нет открытого API «как у Stripe»: приложение просто открывает
 * вашу платёжную ссылку/QR из личного кабинета Kaspi Business, а родитель
 * подтверждает оплату в приложении Kaspi.kz.
 *
 * ЧТО ВСТАВИТЬ:
 *   payUrl — ссылка вашего Kaspi QR / Kaspi Pay из кабинета Kaspi Business.
 *            Обычно вида 'https://kaspi.kz/pay/xxxxx' или ссылка под QR-кодом.
 *            Если ссылка поддерживает подстановку суммы — вставьте шаблон
 *            с {amount}, например 'https://kaspi.kz/pay/AyalaKids?amount={amount}'.
 *   goldPhone — необязательный резервный номер Kaspi Gold для перевода вручную.
 */
export const KASPI = {
  payUrl: 'https://kaspi.kz/pay/REPLACE_ME',
  merchantName: 'Аяла Kids',
  goldPhone: '',
};
